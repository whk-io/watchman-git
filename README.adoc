ifdef::env-github[]
:tip-caption: :bulb:
:note-caption: :information_source:
:important-caption: :heavy_exclamation_mark:
:caution-caption: :fire:
:warning-caption: :warning:
endif::[]

# Watchman-Git 

Makefile and trigger script for rapidly setting up auto git commits on changes with watchman.

TIP: Requires Make, Watchman, Git

* `make` to view the available options
* `make list` : List watchman watched folders
* `make list-triggers` : list triggers for current folder
* `make watch` : Start watching the current folder
* `make watch-stop` : Stops watching the current folder
* `make trigger` : Create trigger for current folder
* `make view-state` : View watchman state
* `make view-log` : View watchman log
* `make monitor-log` : Stream active log
